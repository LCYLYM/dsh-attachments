# Optional character artwork

`character.jpg` is the exact user-supplied image from the 2026-09-14 task. It was not generated, redrawn or downloaded from another source. The source-code MIT license does not assert ownership of this artwork. The default theme uses no image. Replace this file with your own image and run `npm run build` to customize the bundled character theme.
